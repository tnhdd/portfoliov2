import { SccrollSpyDirective } from './sccroll-spy.directive';

describe('SccrollSpyDirective', () => {
  it('should create an instance', () => {
    const directive = new SccrollSpyDirective();
    expect(directive).toBeTruthy();
  });
});
